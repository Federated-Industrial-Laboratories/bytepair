# Pinned to the versions the published numbers were measured against
# (2026-08-05). Changing a pin changes what the audit compares to; do it
# deliberately and re-publish the numbers.
tokenizers==0.22.2
gigatoken==0.10.0
bpe-qwen==0.1.5
transformers==5.14.1
